name: stayline-deps

services:
  deps:
    build:
      context: .
      dockerfile: Dockerfile.dev
      args:
        BUN_VERSION: 1.3.14
    command: bun install --frozen-lockfile
    environment:
      NODE_ENV: development
    volumes:
      - .:/workspace
